/**
 * TypeScript module resolution stub — no runtime code.
 * Metro picks SettingsScreen.ios.tsx or SettingsScreen.android.tsx at bundle time.
 */
import type { ComponentType } from 'react';

declare const SettingsScreen: ComponentType;
export default SettingsScreen;
