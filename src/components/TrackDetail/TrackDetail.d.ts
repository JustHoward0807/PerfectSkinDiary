/**
 * TypeScript module resolution stub. No runtime code.
 *
 * Metro selects the platform implementation automatically:
 *   iOS     → TrackDetail.ios.tsx   (Liquid Glass)
 *   Android → TrackDetail.android.tsx  (DESIGN.md warm palette)
 */
import type { ComponentType } from 'react';

declare const TrackDetail: ComponentType;
export default TrackDetail;
