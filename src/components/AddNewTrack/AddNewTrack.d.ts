/**
 * TypeScript module resolution stub. No runtime code.
 *
 * Metro selects the platform implementation automatically:
 *   iOS     → AddNewTrack.ios.tsx   (Liquid Glass)
 *   Android → AddNewTrack.android.tsx  (DESIGN.md warm palette)
 */
import type { ComponentType } from 'react';

declare const AddNewTrack: ComponentType;
export default AddNewTrack;
